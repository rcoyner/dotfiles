[alias]
    ignored = ls-files --exclude-standard --ignored --others
[color]
    branch = auto
    diff = auto
    interactive = auto
[commit]
    gpgsign = true
[include]
    path = ~/.gitconfig_private
[log]
    decorate = true
